'use strict';

/**
 * @ngdoc service
 * @name SFM.busService
 * @description
 * # busService
 * Factory in the SFM.
 */
angular.module('SFM')
	.factory('busService', ['', function(){
		function getLocation (argument) {
			return ;
		}

		function renderBus() {
			return;
		}

		return {
			renderBus: renderBus
		};
	}])